#include "system/Collision.h"

void Collision::update(entt::registry *registry, TileMap* tileMap)
{

    // get all entity that are solid

    // for each one of them, iterate over


}

bool Collision::detectRectCollision(SDL_Rect *r1, SDL_Rect *r2)
{
    return true;
}
